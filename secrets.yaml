# This file will be encrypted with sops
# After editing, run: sops --encrypt --in-place secrets.yaml

# Admin user password
admin-password: changeme-secure-password

# Database credentials
db-password: database-password-here

# API token for services
api-token: api-token-here

# Add more secrets as needed